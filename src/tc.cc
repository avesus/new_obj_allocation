#include <allocator/slab_layout/thread_cache.h>
#include <stdlib.h>

int main() {
    /*    thread_cache t;
    for(uint32_t i = 0; i < 32; ++i) {
        t.push(rand());
    }
    volatile __m256i _t = t.prep_free<34264, 2, 50, 60>(0);*/
}


// Local Variables:
// rmsbolt-command: "/usr/bin/c++   -I/home/noah/programs/branch_2/new_obj_allocation/lib    -O3 -Wall -Wextra -Wno-unused-function -march=native -mtune=native -mavx2 -std=gnu++1z"
// rmsbolt-disassemble: t
// rmsbolt-asm-format: "att"
// End:

